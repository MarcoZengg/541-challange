# Ignore everything by default
*

# Allow traversing directories (needed for negation patterns in some setups)
!*/

# Only track these at repo root
!.gitignore
!/sp2026_midterm_student_baseline_colab.ipynb
